{
  "name": "lms-diplom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm install --include=dev && npm run build",
    "build:server": "cd server && npm install",
    "postinstall": "npm run build:server && npm run build:client && cd server && npx prisma db push",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}