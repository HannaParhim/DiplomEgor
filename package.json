{
  "name": "lms-diplom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npx vite build",
    "start": "cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}